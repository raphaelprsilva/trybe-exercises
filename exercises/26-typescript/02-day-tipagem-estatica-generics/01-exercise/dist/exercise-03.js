"use strict";
/*
  Ok - Recebe como primeiro parâmetro um array que pode ser de qualquer tipo;
  Ok - Recebe como segundo parâmetro uma função de callback;
  Retorna um novo array removendo os itens que não atendem a condição da função de callback;
  A função de callback recebe como primeiro parâmetro um item do tipo do array;
  A função de callback pode receber como segundo parâmetro um index do tipo inteiro;
  A função de callback pode receber como terceiro parâmetro o próprio array;
  A função de callback devolve um boleano;.
*/
// const arr = [1, 2, 3, 4];
// const filterData = arr2.filter((item) => item > 2);
// console.log(filterData2);
// export function myFilter<T>(array: any[], callback: (value: any) => boolean) {
//   let filterdArray: any[] = [];
//   for (let index = 0; index < array.length; index = index + 1) {
//     if (callback(array[index])) {
//       filterdArray.push(array[index]);
//     }
//   }
//   return filterdArray;
// }
// myFilter([1, 2, 3, 4, 5], (value: any) => value > 2);
